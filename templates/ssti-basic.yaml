id: ssti-basic
info:
  name: Basic SSTI (Server-Side Template Injection)
  severity: high

requests:
  - method: GET
    path:
      - "{{BaseURL}}/?q={{7*7}}"
    matchers:
      - type: word
        words:
          - "49"
        part: body
